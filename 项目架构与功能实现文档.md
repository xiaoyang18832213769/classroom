# 班级管理助手 — 项目架构与功能实现文档

> **用途**：新页面需要相同功能（数据互通、云端同步、图表、统计等）时，直接参考本文档，避免重复踩坑。
>
> **更新日期**：2026-06-23

---

## 一、项目概述

两个独立页面，共享同一个 Supabase 云端数据库，实现跨设备（电脑/手机）、跨网络（WiFi/5G）的实时数据互通。

| 页面 | 文件 | 访问地址 | Supabase 表 |
|------|------|----------|-------------|
| 积分管理 | `index.html` | `/classroom/` | `classroom_data` |
| 签到登记 | `attendance.html` | `/classroom/attendance.html` | `attendance_state` |

**部署方式**：GitHub Pages（静态托管），`ghp_` token 推送至 `xiaoyang18832213769/classroom` 仓库 main 分支。

---

## 二、部署架构

```
用户A(手机5G) ─┐
用户B(电脑WiFi) ─┼─→ GitHub Pages ─→ 加载 HTML/JS/CSS
用户C(平板4G)  ─┘         │
                           │ 页面中的 JS 直接调用
                           ▼
                    Supabase REST API
                    (zhfgvjfuvewbirbvwtrm.supabase.co)
                           │
                    ┌──────┴──────┐
                    │  PostgreSQL │
                    │  classroom_data   (积分)
                    │  attendance_state (签到)
                    └─────────────┘
```

**关键点**：
- GitHub Pages 只托管静态文件，不处理数据
- 数据读写完全通过浏览器 → Supabase 直连
- 不依赖任何本地服务器（`server.py` 已废弃）
- 任何能上网的设备都能独立访问和数据互通

---

## 三、Supabase 数据互通方案（核心）

### 3.1 数据库表结构

```sql
-- 积分管理表
CREATE TABLE classroom_data (
  id INTEGER PRIMARY KEY DEFAULT 1,
  payload JSONB NOT NULL DEFAULT '{}',
  updated_at TIMESTAMPTZ DEFAULT NOW()
);
INSERT INTO classroom_data (id, payload) VALUES (1, '{}');
ALTER TABLE classroom_data DISABLE ROW LEVEL SECURITY;

-- 签到登记表
CREATE TABLE attendance_state (
  id INTEGER PRIMARY KEY DEFAULT 1,
  payload JSONB NOT NULL DEFAULT '{}',
  updated_at TIMESTAMPTZ DEFAULT NOW()
);
INSERT INTO attendance_state (id, payload) VALUES (1, '{}') ON CONFLICT (id) DO NOTHING;
ALTER TABLE attendance_state DISABLE ROW LEVEL SECURITY;
```

**设计要点**：
- 单行设计（`id=1`），所有数据存在 `payload` JSONB 字段中
- `DISABLE ROW LEVEL SECURITY` 必须执行，否则浏览器端写入会被拒（42501 错误）
- `updated_at` 是时间戳比较的关键字段

### 3.2 Supabase 凭证配置

```javascript
// 硬编码默认凭证（所有设备共用）
const SUPABASE_URL = 'https://zhfgvjfuvewbirbvwtrm.supabase.co';
const SUPABASE_KEY = 'eyJhbGci...'; // anon public key
const TABLE = 'your_table_name';
```

- 使用 **anon key**（公开密钥），不是 service_role key
- RLS 禁用后，anon key 即可读写
- 支持从 `localStorage` 读取自定义配置作为覆盖（`sb_url`/`sb_key`）

### 3.3 数据读取函数（模板）

```javascript
async function loadFromCloud() {
  const cfg = getCloudConfig(); if (!cfg) return null;
  try {
    const res = await fetch(`${cfg.url}/rest/v1/${TABLE}?id=eq.1&select=payload,updated_at`, {
      headers: { 'apikey': cfg.key, 'Authorization': `Bearer ${cfg.key}` }
    });
    if (!res.ok) { cloudOnline = false; return null; }
    cloudOnline = true;
    const rows = await res.json();
    if (rows && rows.length > 0 && rows[0].payload) {
      // ⚠️ 关键：使用服务器时间戳，不是 Date.now()
      cloudMtime = rows[0].updated_at ? new Date(rows[0].updated_at).getTime() : Date.now();
      return rows[0].payload;
    }
  } catch(e) { cloudOnline = false; }
  return null;
}
```

### 3.4 数据保存函数（模板）

```javascript
async function saveToCloud(data) {
  const cfg = getCloudConfig(); if (!cfg) return;

  // ⚠️ 预检：防止旧数据覆盖云端新数据
  if (cloudMtime > 0) {
    try {
      const checkRes = await fetch(`${cfg.url}/rest/v1/${TABLE}?id=eq.1&select=updated_at`, {
        headers: { 'apikey': cfg.key, 'Authorization': `Bearer ${cfg.key}` }
      });
      if (checkRes.ok) {
        const rows = await checkRes.json();
        if (rows && rows.length > 0) {
          const serverTs = new Date(rows[0].updated_at).getTime();
          if (serverTs > cloudMtime) {
            // 云端有更新 → 先拉取，丢弃本次保存
            const fresh = await loadFromCloud();
            if (fresh) { appData = fresh; cloudMtime = serverTs; refreshAll(); }
            return;
          }
        }
      }
    } catch(e) {}
  }

  try {
    // ⚠️ 使用 POST + merge-duplicates（UPSERT），不要用 PATCH
    // PATCH 在空表时返回 204 但什么都不做，代码误以为成功
    let res = await fetch(`${cfg.url}/rest/v1/${TABLE}`, {
      method: 'POST',
      headers: {
        'apikey': cfg.key,
        'Authorization': `Bearer ${cfg.key}`,
        'Content-Type': 'application/json',
        'Prefer': 'resolution=merge-duplicates'
      },
      body: JSON.stringify({ id: 1, payload: data, updated_at: new Date().toISOString() })
    });
    if (!res.ok) {
      // 备用：PATCH
      res = await fetch(`${cfg.url}/rest/v1/${TABLE}?id=eq.1`, {
        method: 'PATCH',
        headers: { 'apikey': cfg.key, 'Authorization': `Bearer ${cfg.key}`, 'Content-Type': 'application/json' },
        body: JSON.stringify({ payload: data, updated_at: new Date().toISOString() })
      });
    }
    if (res.ok) {
      // ⚠️ 不要覆盖 cloudMtime！
      if (!cloudOnline) { cloudOnline = true; }
    }
  } catch(e) {
    cloudOnline = false;
  }
}
```

### 3.5 双写策略

```javascript
function saveState() {
  // 本地 + 云端同时保存，确保离线数据不丢失
  localStorage.setItem(STORAGE_KEY, JSON.stringify(state));
  saveToCloud(state);  // async，不 await
}
```

### 3.6 轮询同步（5 秒间隔）

```javascript
// 页面初始化
let cloudMtime = 0; // 0 = 初始/离线状态

// 每 5 秒轮询
setInterval(async () => {
  if (cloudMtime === 0) {
    // 离线加载后，检测恢复连接
    // 尝试推送本地数据
  }
  try {
    const res = await fetch(`${cfg.url}/rest/v1/${TABLE}?id=eq.1&select=updated_at`, {
      headers: { 'apikey': cfg.key, 'Authorization': `Bearer ${cfg.key}` }
    });
    if (!res.ok) return;
    const rows = await res.json();
    if (rows && rows.length > 0) {
      const ts = new Date(rows[0].updated_at).getTime();
      // ⚠️ 不要加 cloudMtime > 0 条件！否则离线恢复后轮询失效
      if (ts > cloudMtime) {
        const data = await loadFromCloud();
        if (data) {
          appData = data;
          cloudMtime = ts;
          refreshAll();
        }
      }
    }
  } catch(e) {}
}, 5000);
```

### 3.7 初始化流程（模板）

```javascript
async function initApp() {
  let cloudData = await loadFromCloud();
  if (cloudData && /* 数据有效 */) {
    appData = cloudData;          // 使用云端数据
    cloudOnline = true;
  } else if (cloudData === null && !cloudOnline) {
    appData = loadFromLocal();    // 离线 → 本地数据
  } else {
    // 云端可到达但无数据 → 首次初始化
    appData = getDefaultData();
    await saveToCloud(appData);
  }
  refreshAll();
}
```

### 3.8 常见踩坑汇总

| 问题 | 原因 | 解决 |
|------|------|------|
| 数据不同步 | `cloudMtime = Date.now()` 用浏览器时间比较服务器时间 | 改用 `rows[0].updated_at` 服务器时间戳 |
| 离线恢复后旧数据覆盖新数据 | `cloudMtime > 0` 条件阻止轮询 | 去掉该条件，只保留 `ts > cloudMtime` |
| PATCH 空表静默失败 | PATCH 无匹配行返回 204，代码判断 `res.ok=true` | 用 POST + `Prefer: resolution=merge-duplicates` |
| RLS 阻止写入 | 新建表默认启用行级安全 | 执行 `ALTER TABLE xxx DISABLE ROW LEVEL SECURITY` |
| 表为空导致初始化失败 | 没有初始行，UPSERT 也不生效 | 插入初始行 `INSERT INTO ... VALUES (1, '{}')` |

---

## 四、积分管理页面（index.html）

### 4.1 数据结构

```javascript
appData = {
  _version: 2,
  students: [{ id: 's1', name: '张三' }, ...],
  points: { 's1': { learning: 0, homework: 0, classroom: 0, contribution: 0 }, ... },
  dailyLogs: {
    '2026-06': {
      '2026-06-23': [
        { studentId: 's1', dimension: 'learning', change: 3, reason: '背诵流利', time: 1234567890 }
      ]
    }
  }
};
```

### 4.2 功能清单

#### 积分管理标签页
- **统计卡片**：班级总积分 / 今日操作数 / 今日涉及学生数
- **学生卡片网格**：每个学生一张卡片，四维积分（学习/作业/课堂/贡献）
  - 教师模式：直接编辑分值
  - 普通模式：+/- 按钮弹出操作面板
- **批量操作**：多选学生 → 统一加减分
- **雷达图 + 柱状图**：Chart.js 绑定学生/维度数据
- **操作记录**：每次加减分记录到 `dailyLogs`，含时间戳和理由

#### 历史总览标签页
- **月历视图**：按日期着色显示积分变动强度
- **日详情**：点击日历日查看当天所有加减分记录
- **月度统计**：四个维度当月净增减
- **排名**：月度揽星状元 + 总分排名 + 各维度排名柱状图

#### 班级管理标签页
- **增删学生**：单个添加 / 批量导入（每行一个姓名）
- **数据管理**：导出 JSON 备份 / 导入恢复 / 重置
- **云端配置**：Supabase URL 和 Key 输入 + 测试连接

### 4.3 关键实现

**加分/减分弹窗**：
```javascript
function changePoints(studentId, dimension, amount, reason) {
  appData.points[studentId][dimension] += amount;
  const today = getToday();
  const log = ensureDailyLog(today);
  log.push({ studentId, dimension, change: amount, reason, time: Date.now() });
  save(); // → saveToStorage → saveToCloud
}
```

**Chart.js 集成**：
```javascript
// 柱状图（维度总览）
new Chart(ctx, {
  type: 'bar',
  data: { labels: ['学习','作业','课堂','贡献'], datasets: [...] },
  options: { responsive: true, maintainAspectRatio: true }
});
// 雷达图（学生个体）
new Chart(ctx, {
  type: 'radar',
  data: { labels: [...], datasets: [{ data: [p.learning, p.homework, ...] }] }
});
```

---

## 五、签到登记页面（attendance.html）

### 5.1 数据结构

```javascript
state = {
  _version: 3,
  classes: {
    '八1班': { no: '1', teacher: '王老师', note: '', count: 45 },
    '八2班': { no: '2', teacher: '李老师', note: '', count: 44 },
    // ... 共 17 个班
  },
  records: {
    '八1班': {
      '2026-26': {  // 周次 key
        '周一': { date: '2026-06-22', expected: 45, actual: 44, absent: [{name:'张三',reason:'病假'}] },
        '周二': { date: '2026-06-23', expected: 45, actual: 0, absent: [] },
        // ...
      }
    }
  }
};
```

**周次计算**：`getWeekKey(date)` → `"2026-26"`（ISO 周数）
**日期映射**：`DAY_NAMES = ['周日','周一','周二','周三','周四','周五','周六']`

### 5.2 功能清单

#### 本周登记标签页

1. **统计卡片**（3 栏布局）：
   - 🏅 全勤班级数（≥95% 且已登记）
   - 📋 今日未到详情（学生姓名 + 原因 + 班级，可滚动，无未到时显示 ✅）
   - 📊 年级平均出勤率

2. **本周累计出勤排名**：横向排列各班名次和出勤率，绿/黄/红按等级着色

3. **今日出勤排名图表**：Chart.js 横向柱状图，只有今天登记了数据的班级显示，≥100% 绿 / ≥90% 黄 / <90% 红

4. **签到提醒条**：
   - 全部已签 → 绿色 ✅
   - 有未签 → 橙色 ⚠️ 列出未签到班级标签
   - 周日附加 "本周数据即将归档" 提示

5. **班级卡片**（每个班一张）：
   - 班序 / 班级名 / 班主任 / 本周出勤率（x/7天）
   - 每天一行：应到/实到输入框 + 出勤率 + 未到人数 + 添加未到按钮
   - 未到学生标签（可点击 × 删除）
   - 备注栏（显示班级备注，可通过 ✎ 编辑）

6. **星期导航**：← 上周 / 本周 / 下周 →
7. **🔄 重置本周**：密码保护（888666）+ 确认对话框 → 所有班级实到清零

#### 历史记录标签页

- **月历视图**：按日期显示班级数和出勤率，节假日标记
- **日详情**：点击日历日 → 展开各班签到详情表格
- **过滤**：只有实到>0 或有未到名单的日期才在日历中显示

#### 班级管理标签页

- **添加班级**：班级名称 / 班序号 / 班主任 / 应到人数
- **批量编辑**：展开表格一次性修改所有班级信息
- **删除班级**：单个删除 + 确认
- **云端状态面板**：测试连接 / 手动同步 / 建表 SQL

### 5.3 关键实现

**出勤率计算**（只统计有记录的天）：
```javascript
[1,2,3,4,5,6,0].forEach(i => { // 周一~周日
  const dr = recs[DAY_NAMES[i]];
  if (dr) { e += dr.expected; a += dr.actual; trackedDays++; }
});
const rate = e > 0 ? Math.round(a / e * 100) : (trackedDays > 0 ? 0 : 100);
```

**签到判定**（实到=0 且无未到名单 → 未签到）：
```javascript
const dr = recs[todayDayName];
if (!dr || (dr.actual === 0 && (!dr.absent || dr.absent.length === 0))) {
  unchecked.push(cn); // 未签到
}
```

**自动保存**（修改应到/实到输入框即触发）：
```javascript
function autoSaveDay(cn, dayIdx) {
  // 从 input 元素读取值 → 更新 state.records → saveState()
}
```

**历史过滤**（只显示有实际数据的日期）：
```javascript
if (dr.date === ds && (dr.actual > 0 || (dr.absent && dr.absent.length > 0))) {
  // 计入统计
}
```

---

## 六、通用技术模式

### 6.1 CSS 设计系统

```css
:root {
  --bg: #f5f0e8;           /* 宣纸底色 */
  --card-bg: #faf8f5;      /* 卡片白 */
  --text: #3d3d3d;         /* 主文字 */
  --text-light: #8c8880;   /* 次要文字 */
  --primary: #426683;      /* 主色（蓝灰） */
  --success: #5b8c5a;      /* 成功（绿） */
  --danger: #c0504d;       /* 危险（红） */
  --border: #e5ddd0;       /* 边框 */
  --shadow: 0 1px 3px rgba(0,0,0,0.06);
  --radius: 10px;
  --font-serif: 'Noto Serif SC', serif;  /* 标题 */
  --font-sans: 'Noto Sans SC', sans-serif; /* 正文 */
}
```

### 6.2 页面模板结构

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>页面标题</title>
  <!-- 字体 -->
  <link href="https://fonts.googleapis.com/css2?family=Noto+Serif+SC:wght@400;600;700&family=Noto+Sans+SC:wght@300;400;500&display=swap" rel="stylesheet">
  <!-- Chart.js -->
  <script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.0/dist/chart.umd.min.js"></script>
  <style>/* CSS */</style>
</head>
<body>
  <nav class="nav"><!-- 导航栏 --></nav>
  <div class="toast-container" id="toastContainer"></div>
  <main class="main">
    <div class="page active" id="page-tab1"><!-- 标签页1 --></div>
    <div class="page" id="page-tab2"><!-- 标签页2 --></div>
  </main>
  <script>/* JS */</script>
</body>
</html>
```

### 6.3 Toast 通知

```javascript
function showToast(msg) {
  const container = document.getElementById('toastContainer');
  const el = document.createElement('div');
  el.className = 'toast'; el.textContent = msg;
  container.appendChild(el);
  setTimeout(() => el.remove(), 2500);
}
```

### 6.4 标签页切换

```javascript
document.querySelectorAll('.nav-tab').forEach(tab => {
  tab.addEventListener('click', () => {
    document.querySelectorAll('.nav-tab').forEach(t => t.classList.remove('active'));
    tab.classList.add('active');
    const page = tab.dataset.page;
    document.querySelectorAll('.page').forEach(p => p.classList.remove('active'));
    document.getElementById(`page-${page}`).classList.add('active');
    // 按需渲染
    if (page === 'tab1') renderTab1();
  });
});
```

### 6.5 密码保护按钮

```javascript
btn.addEventListener('click', () => {
  const pwd = prompt('请输入密码：');
  if (pwd !== '888666') { showToast('❌ 密码错误'); return; }
  // 执行受保护操作
});
```

### 6.6 批量编辑表格

```html
<button id="btnBatchEdit">📝 批量编辑</button>
<div id="batchEditArea" style="display:none;">
  <table>
    <tbody id="batchEditBody">
      <!-- JS 动态生成 input 行 -->
    </tbody>
  </table>
  <button id="btnSaveBatch">💾 保存全部</button>
  <button id="btnCancelBatch">取消</button>
</div>
```

```javascript
// 展开时填充数据
tbody.innerHTML = Object.entries(data).map(([key, item]) => `
  <tr>
    <td>${key}</td>
    <td><input data-key="${key}" data-field="field1" value="${item.field1}"></td>
  </tr>
`).join('');

// 保存时收集所有 input
document.getElementById('btnSaveBatch').addEventListener('click', () => {
  document.querySelectorAll('#batchEditBody input').forEach(inp => {
    const key = inp.dataset.key;
    const field = inp.dataset.field;
    data[key][field] = inp.value;
  });
  saveState();
});
```

---

## 七、新页面快速开发指南

### Step 1：复制模板
复制 `index.html` 或 `attendance.html` 作为起点，保留：
- CSS 变量系统和基础样式
- Supabase 凭证和 `loadFromCloud`/`saveToCloud`/轮询代码
- Toast、导航切换等通用组件

### Step 2：创建 Supabase 表
```sql
CREATE TABLE your_table (
  id INTEGER PRIMARY KEY DEFAULT 1,
  payload JSONB NOT NULL DEFAULT '{}',
  updated_at TIMESTAMPTZ DEFAULT NOW()
);
INSERT INTO your_table (id, payload) VALUES (1, '{}');
ALTER TABLE your_table DISABLE ROW LEVEL SECURITY;
```

### Step 3：修改表名和数据结构
- 改 `TABLE` 常量
- 定义你的 `state`/`appData` 结构
- 实现 `getDefaultData()` 初始数据

### Step 4：实现功能
- 数据操作函数 → 修改 state → 调用 `saveState()`
- 渲染函数 → 读取 state → 生成 HTML
- Chart.js 图表 → 在渲染函数中创建/更新

### Step 5：部署
- `git add` → `git commit` → `git push`（用 token）
- GitHub Pages 自动构建，1-2 分钟后生效
- 访问 `https://xiaoyang18832213769.github.io/classroom/新文件名.html`

### 检查清单
- [ ] `ALTER TABLE xxx DISABLE ROW LEVEL SECURITY` 已执行
- [ ] 表中存在 `id=1` 的初始行
- [ ] `cloudMtime` 使用服务器时间戳，不是 `Date.now()`
- [ ] `saveToCloud` 使用 POST + `merge-duplicates`，不是 PATCH
- [ ] 轮询条件不含 `cloudMtime > 0`
- [ ] 保存前有预检（`serverTs > cloudMtime` → 先拉取）
- [ ] 双写策略（localStorage + cloud 同时保存）
- [ ] 离线加载后轮询能正常工作

---

## 八、关键常量速查

| 常量 | 值 | 说明 |
|------|-----|------|
| Supabase URL | `zhfgvjfuvewbirbvwtrm.supabase.co` | 项目地址 |
| GitHub Pages | `xiaoyang18832213769.github.io/classroom/` | 部署地址 |
| GitHub 仓库 | `xiaoyang18832213769/classroom` | 源码仓库 |
| 重置密码 | `888666` | 签到登记页重置按钮 |
| 轮询间隔 | 5000ms | 云端同步检测频率 |
| 班级数 | 17 | 八1班 ~ 八17班 |

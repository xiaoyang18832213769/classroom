# 揽星阁 · 班级积分 — 设计规范

## 整体定位
中国传统文人审美 + 现代极简主义。调性克制、留白充裕、色彩含蓄，符合语文教师的书卷气质。

---

## 色彩系统

### 主色
| 色名 | 色值 | 用途 |
|------|------|------|
| 月白 | `#f5f0e8` | 页面主背景 |
| 素笺 | `#faf8f5` | 卡片、面板背景 |
| 墨色 | `#3d3d3d` | 主文字色 |
| 茶白 | `#e5ddd0` | 边框、分隔线 |

### 功能色（四维度 + 操作）
| 色名 | 色值 | 用途 |
|------|------|------|
| 黛蓝 | `#426683` | 学习积分 / 主按钮 / 选中态 |
| 秋香 | `#869554` | 作业积分 |
| 胭脂 | `#9b3e46` | 课堂积分 |
| 琥珀 | `#c7823a` | 贡献积分 |
| 竹青 | `#5b8c5a` | 加分操作 / 成功态 |
| 朱砂 | `#c0504d` | 减分操作 / 危险操作 / 警告 |

### 辅助背景色
| 色名 | 色值 | 用途 |
|------|------|------|
| `--learning-bg` | `#e8eef3` | 学习维度标签底色 |
| `--homework-bg` | `#f2f4ec` | 作业维度标签底色 |
| `--classroom-bg` | `#f7edee` | 课堂维度标签底色 |
| `--contribution-bg` | `#faf3ea` | 贡献维度标签底色 |
| `--add-bg` | `#edf4ed` | 加分按钮 hover |
| `--subtract-bg` | `#f8eeee` | 减分按钮 hover |

---

## 排版

### 字体
| 层级 | 字体 | 大小 | 粗细 |
|------|------|------|------|
| 页面标题 | Noto Serif SC（衬线） | 1.35rem | 700 |
| 章节标题 | Noto Serif SC | 1.1rem | 600 |
| 卡片姓名 | Noto Serif SC | 1.25rem | 600 |
| 数值大字 | Noto Serif SC | 1.5–2rem | 700 |
| 正文/标签 | Noto Sans SC（无衬线） | 0.85–0.95rem | 400–500 |
| 辅助文字 | Noto Sans SC | 0.75–0.8rem | 400 |

### 字间距
- 标题类：`letter-spacing: 0.05–0.08em`
- 正文：默认

---

## 间距与尺寸

| 元素 | 值 |
|------|-----|
| 页面最大宽度 | `1280px` |
| 页面内边距 | `2rem`（桌面）/ `1rem`（手机） |
| 卡片圆角 | `10px`（大）/ `6px`（小）/ `16px`（特大） |
| 卡片间距 | `1.25rem` |
| 卡片内边距 | `1.5rem` |
| 导航高度 | `60px` |
| 小按钮尺寸 | `28×28px`（±按钮） |

---

## 阴影与层次

| 层次 | 值 | 用途 |
|------|-----|------|
| 卡片 | `0 1px 3px rgba(0,0,0,0.06)` | 默认卡片 |
| 悬停 | `0 4px 16px rgba(0,0,0,0.08)` | 卡片 hover |
| 弹窗 | `0 12px 40px rgba(0,0,0,0.12)` | 模态弹窗 |
| 底部栏 | `0 -4px 20px rgba(0,0,0,0.08)` | 批量操作栏 |

**原则：阴影极淡，几乎不可见，只在交互时加深。**

---

## 组件规范

### 卡片
```css
background: var(--card-bg);  /* #faf8f5 */
border-radius: var(--radius-lg);  /* 16px */
padding: 1.5rem;
box-shadow: var(--shadow);  /* 极淡阴影 */
border: 1px solid transparent;
transition: 0.2s ease;
```
- hover 时：阴影加深 + 边框显色
- 内容分区用 `border-bottom: 1px solid var(--border-light)`

### 按钮
**主按钮**
```css
background: var(--learning);  /* 黛蓝 */
color: white;
border: none;
border-radius: 6px;
padding: 0.55–0.65rem 1.25rem;
font-weight: 500;
```

**次按钮 / 取消**
```css
background: var(--border);  /* 茶白 */
color: var(--text);
```

**危险按钮**
```css
background: var(--subtract);  /* 朱砂 */
color: white;
```

**小按钮（± 符号）**
```css
width: 28px; height: 28px;
border: 1px solid var(--border);
background: white;
border-radius: 6px;
```
- hover 加减时分别变竹青/朱砂色调

### 输入框
```css
border: 1px solid var(--border);
border-radius: 6px;
padding: 0.6rem 0.85rem;
background: white;
```
- focus：边框变黛蓝 + 浅色光晕 `0 0 0 2px rgba(66,102,131,0.15)`

### 标签/徽章
```css
display: inline-block;
padding: 0.15rem 0.55rem;
border-radius: 100px;  /* 胶囊形 */
font-size: 0.78rem;
```
- 学习：`bg:#e8eef3, color:#426683`
- 作业：`bg:#f2f4ec, color:#869554`
- 课堂：`bg:#f7edee, color:#9b3e46`
- 贡献：`bg:#faf3ea, color:#c7823a`

### 弹窗
- 居中遮罩 `background: rgba(0,0,0,0.2)`
- 白色卡片 + 大阴影，宽度 340px
- 入场动画 `@keyframes popIn { scale(0.95→1) + opacity }`

### Toast 提示
- 固定在右上角
- 墨色背景 + 白色文字
- 入场从右滑入 `@keyframes slideIn`

### 导航栏
- 粘性定位
- 毛玻璃效果 `backdrop-filter: blur(16px)`
- 半透明背景 `rgba(250,248,245,0.85)`
- 底部细线分隔

---

## 布局

### 网格系统
- 统计卡片：`grid-template-columns: repeat(3, 1fr)`
- 学生卡片：`grid-template-columns: repeat(auto-fill, minmax(340px, 1fr))`
- 图表区域：`grid-template-columns: 1fr 1fr`
- 月度摘要：`grid-template-columns: repeat(4, 1fr)`

### 响应式断点
| 宽度 | 变化 |
|------|------|
| ≤768px | 网格改单列、导航换行、图表堆叠 |
| ≤480px | 导航标签缩小、日历间距缩小 |

---

## 动画

| 场景 | 动画 | 时长 |
|------|------|------|
| 页面切换 | `fadeIn`（opacity + translateY 8px→0） | 0.3s |
| 弹窗打开 | `popIn`（scale 0.95→1 + opacity） | 0.2s |
| Toast 入场 | `slideIn`（translateX 20px→0 + opacity） | 0.3s |
| 批量栏入场 | `slideUp`（translateY 100%→0） | 0.25s |
| 按钮悬停 | `transition: 0.2s ease` | 0.2s |

**原则：过渡动画统一使用 `cubic-bezier(0.4, 0, 0.2, 1)`**

---

## 设计原则

1. **大面积留白** — 内容密度低，卡片之间慷慨留空
2. **少用线条** — 分区首选留白和色块，而非框线
3. **颜色克制** — 一页不超过 3–4 种主色，功能色仅用于突出信息
4. **字体分级** — 衬线体专用于标题和姓名，无衬线体用于正文
5. **阴影极浅** — 避免浮夸的投影，营造纸质感而非 3D 感
6. **图标少而精** — emoji 作为点缀，避免过度装饰
7. **过渡丝滑** — 所有交互都有 0.2s 过渡，不突兀

---

## 章节标题装饰

每个区块标题左侧搭配 4px 宽竖线：
```css
.xxx-title::before {
  content: '';
  width: 4px; height: 18px;
  border-radius: 2px;
  background: var(--learning);  /* 黛蓝 */
}
```

---

## 适用场景
此规范适用于"揽星阁"系列所有功能页面：积分管理、历史总览、班级管理、排行榜，
以及未来扩展的通知管理、作业管理、家校沟通等模块。保持一致的配色、间距、
字体和动画规则即可无缝融入现有风格。
